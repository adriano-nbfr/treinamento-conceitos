import { FiltroHover } from './filtro-hover';

describe('FiltroHover', () => {
  it('should create an instance', () => {
    const directive = new FiltroHover();
    expect(directive).toBeTruthy();
  });
});
